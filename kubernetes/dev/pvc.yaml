apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: interlogica-pvc
spec:
  accessModes:
    - ReadWriteOnce
#  storageClassName: local-storage
  storageClassName: manual
  resources:
    requests:
      storage: 2Gi
